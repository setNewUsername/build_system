import 'package:flutter/material.dart';
import 'package:<project_name>/commons/module_base.dart';

class <module_id> extends StatelessWidget with ModuleBase{
  final String moduleParentScreenId; 

  const <module_id>({super.key, required this.moduleParentScreenId});

  @override
  Widget build(BuildContext context){
    return CustomScrollView(
      slivers: [
        SliverGrid(
          delegate: SliverChildBuilderDelegate(((context, index) {
            return
            <children_lines>
          }), <children_amount>),
          gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
            crossAxisCount: <columns_amount>,
            mainAxisSpacing: <grid_columns_gap>,
            crossAxisSpacing: <grid_row_gap>
          )
        ),
      ],
    );
  }
}